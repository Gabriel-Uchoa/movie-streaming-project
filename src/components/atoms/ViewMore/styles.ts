import styled from "styled-components";

export const TextViewMore = styled.span`
    color: #FED530;
    cursor: pointer;
    :hover {
        text-shadow: 0.1em 0.1em 0.2em black;
        transition-duration: 0.5s;
    }
`